# Generated by `atlas docs reconcile`; edit Atlas delivery intent, not this file.
name: Documentation façade

on:
  workflow_dispatch:
    inputs:
      website_sha:
        description: Exact Website commit already live at the organization root
        required: true
        type: string

permissions: {}

concurrency:
  group: b10x-docs-pages
  cancel-in-progress: false

jobs:
  publish:
    if: >-
      github.actor == 'b10x-bot[bot]'
      && github.triggering_actor == 'b10x-bot[bot]'
      && github.repository == 'beyond10x/worktree'
      && github.ref == 'refs/heads/main'
    permissions:
      contents: read
      pages: write
      id-token: write
    uses: beyond10x/website/.github/workflows/redirect-facade.yml@1d46983fc74643bd21c1ae06a87c444e706f17a0
    with:
      website_sha: ${{ inputs.website_sha }}
      repository: worktree
      control_sha: ${{ github.sha }}
