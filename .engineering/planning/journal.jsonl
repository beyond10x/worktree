{"entity":"story","version":1,"id":"scope-flag-for-reporting-subcommands","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Problem\n\n`worktree gc --repo <path>` and `worktree status` both report far more than the repository the\noperator is working in, and nothing in their output marks the difference.\n\n`--repo` is documented as \"Repository used to select its activated workspace policy\"\n(`worktree gc --help`), so it resolves a profile from `~/.config/worktree/config.toml` and then\nassesses every record under that profile's `workspace_root`. Observed on 2026-09-02 with\n`b10x-worktree-cli` 0.3.1:\n\n- `worktree gc --repo /home/timo/beyond10x/agentplugins --dry-run` returned 31 assessments, 28 of\n  them eligible, spanning atlas, connectors, aep, ess, harness, todo, website, service-sdk and\n  devcenter. One belonged to agentplugins.\n- `worktree gc --repo /home/timo/babelforce/projects/sbf/acd --dry-run` returned 1 assessment, and\n  that record's `repository_root` was `/home/timo/babelforce/projects/devcenter` — a different\n  repository inside the same `babelforce` profile.\n- `worktree status --help` lists one option, `--json`. It returned all 297 records across the\n  `b10x`, `babelforce` and `default` tree roots.\n\nAn agent following the generated skill's own step — run the dry-run and inspect every result —\ntherefore reads a list dominated by other repositories' trees. An `--apply` issued without exact\n`--id` values would remove another repository's work.\n\n## Proposal\n\n1. Add a `--scope` flag with three values:\n   - `repo` — only records whose `repository_root` is the resolved repository.\n   - `profile` — every record under the resolved profile's `workspace_root`. This is today's\n     behaviour.\n   - `global` — every record in the registry, across every profile.\n2. Default `--scope` to `repo` when the command is invoked from inside a repository, so the common\n   case narrows without a flag.\n3. Apply it to `gc`, `status` and `reconcile` at least.\n4. Leave `--repo` meaning what it means today — profile resolution. `--scope` decides how much of\n   that profile is reported.\n\n## Acceptance\n\n- `worktree gc --dry-run` from inside a repository assesses only that repository's records.\n- `--scope profile` reproduces today's output byte for byte on the same registry.\n- `--scope global` on `status` returns every record, matching today's unfiltered `status`.\n- `worktree status --scope repo` in a repository with no managed trees returns an empty set rather\n  than the whole registry.\n- The generated skill states the default and the flag.\n\n## Evidence\n\nWorktree 0.3.2 documents the current behaviour in the generated skill\n(`crates/worktree-cli/src/main.rs`, \"Finish and clean up\" step 3 and \"Audit and recovery\"). It\nrecords the limitation; it does not change it.\n","summary":"gc and status report every record in the whole workspace profile, so add a --scope flag with repo, profile and global, and default to repo.","title":"Scope reporting and cleanup subcommands to the repository by default"},"args":{"command":"create-entity","data":{"body":"## Problem\n\n`worktree gc --repo <path>` and `worktree status` both report far more than the repository the\noperator is working in, and nothing in their output marks the difference.\n\n`--repo` is documented as \"Repository used to select its activated workspace policy\"\n(`worktree gc --help`), so it resolves a profile from `~/.config/worktree/config.toml` and then\nassesses every record under that profile's `workspace_root`. Observed on 2026-09-02 with\n`b10x-worktree-cli` 0.3.1:\n\n- `worktree gc --repo /home/timo/beyond10x/agentplugins --dry-run` returned 31 assessments, 28 of\n  them eligible, spanning atlas, connectors, aep, ess, harness, todo, website, service-sdk and\n  devcenter. One belonged to agentplugins.\n- `worktree gc --repo /home/timo/babelforce/projects/sbf/acd --dry-run` returned 1 assessment, and\n  that record's `repository_root` was `/home/timo/babelforce/projects/devcenter` — a different\n  repository inside the same `babelforce` profile.\n- `worktree status --help` lists one option, `--json`. It returned all 297 records across the\n  `b10x`, `babelforce` and `default` tree roots.\n\nAn agent following the generated skill's own step — run the dry-run and inspect every result —\ntherefore reads a list dominated by other repositories' trees. An `--apply` issued without exact\n`--id` values would remove another repository's work.\n\n## Proposal\n\n1. Add a `--scope` flag with three values:\n   - `repo` — only records whose `repository_root` is the resolved repository.\n   - `profile` — every record under the resolved profile's `workspace_root`. This is today's\n     behaviour.\n   - `global` — every record in the registry, across every profile.\n2. Default `--scope` to `repo` when the command is invoked from inside a repository, so the common\n   case narrows without a flag.\n3. Apply it to `gc`, `status` and `reconcile` at least.\n4. Leave `--repo` meaning what it means today — profile resolution. `--scope` decides how much of\n   that profile is reported.\n\n## Acceptance\n\n- `worktree gc --dry-run` from inside a repository assesses only that repository's records.\n- `--scope profile` reproduces today's output byte for byte on the same registry.\n- `--scope global` on `status` returns every record, matching today's unfiltered `status`.\n- `worktree status --scope repo` in a repository with no managed trees returns an empty set rather\n  than the whole registry.\n- The generated skill states the default and the flag.\n\n## Evidence\n\nWorktree 0.3.2 documents the current behaviour in the generated skill\n(`crates/worktree-cli/src/main.rs`, \"Finish and clean up\" step 3 and \"Audit and recovery\"). It\nrecords the limitation; it does not change it.\n","status":"draft","summary":"gc and status report every record in the whole workspace profile, so add a --scope flag with repo, profile and global, and default to repo.","title":"Scope reporting and cleanup subcommands to the repository by default"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/scope-flag-for-reporting-subcommands"},"payload":{"actor":"human:timo","at":1788350187079,"causation":"cmd-new-story-scope-flag-for-reporting-subcommands","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:scope-flag-for-reporting-subcommands@1#0~1ee207b6a9d1c7b3","executor":null,"recorded_at":"2026-09-02T11:56:27Z"}}
{"entity":"story","version":1,"id":"worktree-diff","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: `worktree diff` — what a managed worktree holds that its base does not\n\n## Goal\nOne command answers, for a managed worktree, *what did this tree add on top of the revision it was\ncreated from* — the commits, each commit's file list with insertion/deletion counts, and the diff\nof those commits, optionally narrowed to a path — so an operator or an agent reviewing a hand-off\ndoes not assemble it from `git show --stat`, `git diff <base>..HEAD` and a path filter by hand.\n\nThe operator's own assembly of it, 2026-09-04, which this command replaces:\n\n```\ncd <managed path>\ngit show <commit> --stat --format='%h %s' | tail -20\ngit show <commit> -- crates/entity-store/src/file.rs | grep -E '^[-+]' | grep -vE '^(\\+\\+\\+|---)'\n```\n\n## Shape\n- `worktree diff [PATH] [--base <rev>] [--path <p>]... [--stat] [--json]`.\n- Default base: the revision recorded in the registry at `create` (`--base` overrides); default\n  range `<base>..HEAD`.\n- `--stat` prints commits and per-file counts only; without it, the unified diff follows the stat.\n- `--json` emits the same as data: commits (hash, subject, author, committer), files (path,\n  insertions, deletions, status), and the patch text per file.\n- A worktree whose HEAD equals its base prints that and exits 0; an unmanaged path is refused by\n  name, like every other verb.\n\n## Acceptance\n- On a worktree with two commits over its base, `worktree diff --stat` lists both commits and every\n  changed file with counts; `worktree diff --path <one file>` prints only that file's patch.\n- `--json` round-trips through the project's stable-output tests like `list --json`.\n- The command reads only; it never fetches, checks out or writes.\n","summary":"One read-only verb showing what a managed worktree holds on top of the revision it was created from: commits, per-file stat, patch, optional path filter, JSON form.","title":"worktree diff: the commits, file list and patch a managed worktree adds over its base"},"args":{"command":"create-entity","data":{"body":"# Story: `worktree diff` — what a managed worktree holds that its base does not\n\n## Goal\nOne command answers, for a managed worktree, *what did this tree add on top of the revision it was\ncreated from* — the commits, each commit's file list with insertion/deletion counts, and the diff\nof those commits, optionally narrowed to a path — so an operator or an agent reviewing a hand-off\ndoes not assemble it from `git show --stat`, `git diff <base>..HEAD` and a path filter by hand.\n\nThe operator's own assembly of it, 2026-09-04, which this command replaces:\n\n```\ncd <managed path>\ngit show <commit> --stat --format='%h %s' | tail -20\ngit show <commit> -- crates/entity-store/src/file.rs | grep -E '^[-+]' | grep -vE '^(\\+\\+\\+|---)'\n```\n\n## Shape\n- `worktree diff [PATH] [--base <rev>] [--path <p>]... [--stat] [--json]`.\n- Default base: the revision recorded in the registry at `create` (`--base` overrides); default\n  range `<base>..HEAD`.\n- `--stat` prints commits and per-file counts only; without it, the unified diff follows the stat.\n- `--json` emits the same as data: commits (hash, subject, author, committer), files (path,\n  insertions, deletions, status), and the patch text per file.\n- A worktree whose HEAD equals its base prints that and exits 0; an unmanaged path is refused by\n  name, like every other verb.\n\n## Acceptance\n- On a worktree with two commits over its base, `worktree diff --stat` lists both commits and every\n  changed file with counts; `worktree diff --path <one file>` prints only that file's patch.\n- `--json` round-trips through the project's stable-output tests like `list --json`.\n- The command reads only; it never fetches, checks out or writes.\n","status":"draft","summary":"One read-only verb showing what a managed worktree holds on top of the revision it was created from: commits, per-file stat, patch, optional path filter, JSON form.","title":"worktree diff: the commits, file list and patch a managed worktree adds over its base"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/worktree-diff"},"payload":{"actor":"human:timo","at":1788475751823,"causation":"cmd-new-story-worktree-diff","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:worktree-diff@1#0~f4c8a60bcacdf29a","executor":null,"recorded_at":"2026-09-03T22:49:11Z"}}
{"entity":"story","version":1,"id":"rebase-head-is-not-a-lock","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: a stale `REBASE_HEAD` is not a paused rebase\n\n## Defect\n`has_operational_lock` (`crates/worktree-git/src/lib.rs:380`) treats the presence of any of\n`rebase-merge`, `rebase-apply`, `sequencer`, `MERGE_HEAD`, `CHERRY_PICK_HEAD`, `REVERT_HEAD`,\n`REBASE_HEAD` in the worktree's git directory as a paused sequencer, and `finish` then refuses\n`worktree-locked: Git marks the worktree locked`. Git keeps `REBASE_HEAD` after a rebase has\n**completed** (`git rebase --continue` to the end leaves the file; only the `rebase-merge` /\n`rebase-apply` directory says a rebase is in progress). Measured 2026-09-04 on two managed\nworktrees (connectors, aep) after `git rebase origin/main` + `--continue`: `git worktree list\n--porcelain` reported no lock, `rebase-merge` and `rebase-apply` were absent, `REBASE_HEAD` and\n`ORIG_HEAD` were present, and `finish` refused both until `REBASE_HEAD` was deleted by hand. The\nrefusal names Git as the source of a lock Git does not hold.\n\n## Shape\n- A rebase is in progress iff `rebase-merge` or `rebase-apply` exists; a cherry-pick or revert iff\n  `sequencer`, `CHERRY_PICK_HEAD` or `REVERT_HEAD`; a merge iff `MERGE_HEAD`. `REBASE_HEAD` alone is\n  a stale marker and not a lock.\n- The refusal names the marker it found (`rebase in progress: rebase-merge`), never \"Git marks the\n  worktree locked\" for a state Git does not report.\n- `worktree doctor` reports a stale `REBASE_HEAD` as a note, not a refusal.\n\n## Acceptance\n- A worktree whose rebase completed (no `rebase-merge`/`rebase-apply`, `REBASE_HEAD` present)\n  finishes.\n- A worktree with `rebase-merge` present is refused naming the directory.\n- The existing refusals for `MERGE_HEAD`, `CHERRY_PICK_HEAD`, `REVERT_HEAD`, `sequencer` are unchanged.\n","title":"A stale REBASE_HEAD is not a paused rebase"},"args":{"command":"create-entity","data":{"body":"# Story: a stale `REBASE_HEAD` is not a paused rebase\n\n## Defect\n`has_operational_lock` (`crates/worktree-git/src/lib.rs:380`) treats the presence of any of\n`rebase-merge`, `rebase-apply`, `sequencer`, `MERGE_HEAD`, `CHERRY_PICK_HEAD`, `REVERT_HEAD`,\n`REBASE_HEAD` in the worktree's git directory as a paused sequencer, and `finish` then refuses\n`worktree-locked: Git marks the worktree locked`. Git keeps `REBASE_HEAD` after a rebase has\n**completed** (`git rebase --continue` to the end leaves the file; only the `rebase-merge` /\n`rebase-apply` directory says a rebase is in progress). Measured 2026-09-04 on two managed\nworktrees (connectors, aep) after `git rebase origin/main` + `--continue`: `git worktree list\n--porcelain` reported no lock, `rebase-merge` and `rebase-apply` were absent, `REBASE_HEAD` and\n`ORIG_HEAD` were present, and `finish` refused both until `REBASE_HEAD` was deleted by hand. The\nrefusal names Git as the source of a lock Git does not hold.\n\n## Shape\n- A rebase is in progress iff `rebase-merge` or `rebase-apply` exists; a cherry-pick or revert iff\n  `sequencer`, `CHERRY_PICK_HEAD` or `REVERT_HEAD`; a merge iff `MERGE_HEAD`. `REBASE_HEAD` alone is\n  a stale marker and not a lock.\n- The refusal names the marker it found (`rebase in progress: rebase-merge`), never \"Git marks the\n  worktree locked\" for a state Git does not report.\n- `worktree doctor` reports a stale `REBASE_HEAD` as a note, not a refusal.\n\n## Acceptance\n- A worktree whose rebase completed (no `rebase-merge`/`rebase-apply`, `REBASE_HEAD` present)\n  finishes.\n- A worktree with `rebase-merge` present is refused naming the directory.\n- The existing refusals for `MERGE_HEAD`, `CHERRY_PICK_HEAD`, `REVERT_HEAD`, `sequencer` are unchanged.\n","status":"draft","title":"A stale REBASE_HEAD is not a paused rebase"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/rebase-head-is-not-a-lock"},"payload":{"actor":"agent:org-brain","at":1788488163351,"causation":"cmd-new-story-rebase-head-is-not-a-lock","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:rebase-head-is-not-a-lock@1#0~518c92e42fae8ddb","executor":null,"recorded_at":"2026-09-04T02:16:03Z"}}
{"entity":"story","version":1,"id":"native-worktree-storage-inspection","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Outcome\n\nAn operator investigating disk pressure can run `worktree inspect --repo <path>` to see current worktree state, occupied storage and why retention continues. Repository scope is the default; `--workspace` explicitly expands to its activated profile, and repeatable `--id` narrows that scope. Inspection includes active records that ordinary GC omits until expiry.\n\n## Evidence and contract\n\nThe 7 September disk audit found substantial duplicate compiler output, registry HEADs behind actual HEADs, clean published work still recorded active, almost no maintained leases, and ignored raw data inside a completed-story tree. `crates/worktree/src/lib.rs` separates finish and GC and filters young active trees from GC; `crates/worktree-git/src/lib.rs` treats ignored state as dirty. Those are inspection inputs, not permission to discard files.\n\nThe report is a new separately named `worktree.inspection/1` surface inside the existing CLI envelope. Existing JSON, hook, configuration and registry formats retain their bytes and meaning. New domain observation: `.engineering/specs/worktree-inspection/system.yaml`, validated with the installed ESS CLI.\n\n## Scope and acceptance\n\n- Public library orchestration owns selection and assessment; CLI renders results.\n- Show record purpose/owner/lifecycle/recorded HEAD alongside actual HEAD and branch, separate tracked/untracked/ignored counts, live leases and observed age. No lease is not proof that an owner has abandoned work.\n- Count logical and allocated storage where supported, deduplicate hard links, do not follow symlinks, and bound traversal. Partial observations are explicit and never reported as complete zero usage.\n- Include missing/unreadable paths and Git failures as per-record findings; one bad tree must not hide the rest.\n- Default inspection performs no network calls or lifecycle writes. `--refresh` explicitly obtains current advertised remote-ref recovery facts, may fetch missing Git objects, and detects HEAD changes during the proof.\n- Show finish/expiry blockers and preserve current GC/removal gates. Inspection does not authorize deletion, infer completion from age or modify story/issue states.\n- Task association remains explicitly unavailable in the current registry: free-text purpose is shown, but changed planning files and coincident tokens are not promoted to ownership. Consumer integrations must eventually supply explicit references and observed status through typed adapters above this portable library.\n- `task check` plus process-level tests cover repository versus workspace scope, ignored evidence, partial size, symlink/hard-link accounting, missing paths, fresh recovery and unchanged lifecycle state.\n\n## Follow-through required in consumers\n\nThis command makes retention visible; it does not by itself prevent accumulation. Harness/plugin owners must wire session leases and an explicit end-of-work disposition; wave owners must record publication/handoff and finish/GC outcomes. Build/scratch owners must declare disposable resources separately from retained evidence, with bounded budgets and expiry. Do not archive entire Cargo targets as evidence. These cross-repository changes require their own governed implementation work and are not represented as delivered by this story.\n\n## Recovery policy\n\nExact currently advertised remote branches or tags protect detached/local-only commits after checkout retirement. A local branch or verified bundle could support a separately designed recovery mode; neither is admitted by current removal policy. Build cleanup must be independently available so unpublished source does not force retention of disposable gigabytes.\n","title":"Inspect storage and retention blockers through the worktree facade"},"args":{"command":"create-entity","data":{"body":"## Outcome\n\nAn operator investigating disk pressure can run `worktree inspect --repo <path>` to see current worktree state, occupied storage and why retention continues. Repository scope is the default; `--workspace` explicitly expands to its activated profile, and repeatable `--id` narrows that scope. Inspection includes active records that ordinary GC omits until expiry.\n\n## Evidence and contract\n\nThe 7 September disk audit found substantial duplicate compiler output, registry HEADs behind actual HEADs, clean published work still recorded active, almost no maintained leases, and ignored raw data inside a completed-story tree. `crates/worktree/src/lib.rs` separates finish and GC and filters young active trees from GC; `crates/worktree-git/src/lib.rs` treats ignored state as dirty. Those are inspection inputs, not permission to discard files.\n\nThe report is a new separately named `worktree.inspection/1` surface inside the existing CLI envelope. Existing JSON, hook, configuration and registry formats retain their bytes and meaning. New domain observation: `.engineering/specs/worktree-inspection/system.yaml`, validated with the installed ESS CLI.\n\n## Scope and acceptance\n\n- Public library orchestration owns selection and assessment; CLI renders results.\n- Show record purpose/owner/lifecycle/recorded HEAD alongside actual HEAD and branch, separate tracked/untracked/ignored counts, live leases and observed age. No lease is not proof that an owner has abandoned work.\n- Count logical and allocated storage where supported, deduplicate hard links, do not follow symlinks, and bound traversal. Partial observations are explicit and never reported as complete zero usage.\n- Include missing/unreadable paths and Git failures as per-record findings; one bad tree must not hide the rest.\n- Default inspection performs no network calls or lifecycle writes. `--refresh` explicitly obtains current advertised remote-ref recovery facts, may fetch missing Git objects, and detects HEAD changes during the proof.\n- Show finish/expiry blockers and preserve current GC/removal gates. Inspection does not authorize deletion, infer completion from age or modify story/issue states.\n- Task association remains explicitly unavailable in the current registry: free-text purpose is shown, but changed planning files and coincident tokens are not promoted to ownership. Consumer integrations must eventually supply explicit references and observed status through typed adapters above this portable library.\n- `task check` plus process-level tests cover repository versus workspace scope, ignored evidence, partial size, symlink/hard-link accounting, missing paths, fresh recovery and unchanged lifecycle state.\n\n## Follow-through required in consumers\n\nThis command makes retention visible; it does not by itself prevent accumulation. Harness/plugin owners must wire session leases and an explicit end-of-work disposition; wave owners must record publication/handoff and finish/GC outcomes. Build/scratch owners must declare disposable resources separately from retained evidence, with bounded budgets and expiry. Do not archive entire Cargo targets as evidence. These cross-repository changes require their own governed implementation work and are not represented as delivered by this story.\n\n## Recovery policy\n\nExact currently advertised remote branches or tags protect detached/local-only commits after checkout retirement. A local branch or verified bundle could support a separately designed recovery mode; neither is admitted by current removal policy. Build cleanup must be independently available so unpublished source does not force retention of disposable gigabytes.\n","status":"draft","title":"Inspect storage and retention blockers through the worktree facade"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/native-worktree-storage-inspection"},"payload":{"actor":"human:timo","at":1788780701952,"causation":"cmd-new-story-native-worktree-storage-inspection","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:native-worktree-storage-inspection@1#0~b8fd61d4a5e7a86d","executor":null,"recorded_at":"2026-09-07T11:31:41Z"}}
{"entity":"story","version":1,"id":"native-worktree-storage-inspection","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"informed_by":"story:scope-flag-for-reporting-subcommands"}]},"args":{"command":"create-relation","kind":"informed_by","source":"01MEM0000000000000010","target":"01MEM0000000000000004"},"payload":{"actor":"human:timo","at":1788780701963,"causation":"cmd-story-native-worktree-storage-inspection-informed_by-story-scope-flag-for-reporting-subcommands","change":{"change":"related","relation":"informed_by","target":"story:scope-flag-for-reporting-subcommands"},"correlation":"protocol-artifact-relate","event_id":"story:native-worktree-storage-inspection@1#0~5d305770f8eeaae0","executor":null,"recorded_at":"2026-09-07T11:31:41Z"}}
{"entity":"story","version":1,"id":"native-worktree-storage-inspection","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"informed_by":"story:scope-flag-for-reporting-subcommands"},{"informed_by":"story:rebase-head-is-not-a-lock"}]},"args":{"command":"create-relation","kind":"informed_by","source":"01MEM0000000000000010","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788780701975,"causation":"cmd-story-native-worktree-storage-inspection-informed_by-story-rebase-head-is-not-a-lock","change":{"change":"related","relation":"informed_by","target":"story:rebase-head-is-not-a-lock"},"correlation":"protocol-artifact-relate","event_id":"story:native-worktree-storage-inspection@1#0~31d38a70e05b79db","executor":null,"recorded_at":"2026-09-07T11:31:41Z"}}
{"entity":"story","version":1,"id":"native-worktree-storage-inspection","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000001","to":"proposed"},"payload":{"actor":"human:timo","at":1788780702225,"causation":"cmd-move-story-native-worktree-storage-inspection-1788780702225","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:native-worktree-storage-inspection@2#0~081adaf6578f49c7","executor":null,"recorded_at":"2026-09-07T11:31:42Z"}}
{"entity":"story","version":1,"id":"native-worktree-storage-inspection","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000001","to":"active"},"payload":{"actor":"human:timo","at":1788780702366,"causation":"cmd-move-story-native-worktree-storage-inspection-1788780702366","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:native-worktree-storage-inspection@3#0~8c7e859d3507c067","executor":null,"recorded_at":"2026-09-07T11:31:42Z"}}
{"entity":"story","version":1,"id":"native-worktree-storage-inspection","revision":3,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"/home/timo/.cache/disk-audit-20260907-414yqwj_/worktree-inspect-gate.log","source":"task check: formatting, Clippy, 69 tests, generated skill; exit 0","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788781444000,"causation":"cmd-evidence-story-native-worktree-storage-inspection-test_result-1788781444864","change":{"change":"evidence","kind":"test_result","reference":"/home/timo/.cache/disk-audit-20260907-414yqwj_/worktree-inspect-gate.log","source":"task check: formatting, Clippy, 69 tests, generated skill; exit 0"},"correlation":"protocol-artifact-evidence","event_id":"story:native-worktree-storage-inspection@3#0~f7b2cdccb7cef770","executor":null,"recorded_at":"2026-09-07T11:44:04Z"}}
{"entity":"story","version":1,"id":"native-worktree-storage-inspection","revision":3,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"/home/timo/.cache/disk-audit-20260907-414yqwj_/native-inspection-b10x.json","source":"Native workspace inspection completed against the real activated profile; exit 0","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788781444000,"causation":"cmd-evidence-story-native-worktree-storage-inspection-test_result-1788781444937","change":{"change":"evidence","kind":"test_result","reference":"/home/timo/.cache/disk-audit-20260907-414yqwj_/native-inspection-b10x.json","source":"Native workspace inspection completed against the real activated profile; exit 0"},"correlation":"protocol-artifact-evidence","event_id":"story:native-worktree-storage-inspection@3#0~d8382655ceec6986","executor":null,"recorded_at":"2026-09-07T11:44:04Z"}}
{"entity":"story","version":1,"id":"native-worktree-storage-inspection","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":2}}","target":"01MEM0000000000000001","to":"implemented"},"payload":{"actor":"human:timo","at":1788781445071,"causation":"cmd-move-story-native-worktree-storage-inspection-1788781445071","change":{"change":"moved","decided_on":{"recorded":{"test_result":2}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:native-worktree-storage-inspection@4#0~9efb23f9f8c293cd","executor":null,"recorded_at":"2026-09-07T11:44:05Z"}}
{"entity":"task","version":1,"id":"stale-rebase-cleanup-correction","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Bounded correction\n\nThe current disk cleanup reproduced 15 false lock refusals whose only worktree-local operation marker is REBASE_HEAD. An existing independently owned managed checkout contains the narrow Git-adapter correction and regression test. Reuse that reviewed patch in this integration branch without changing its original checkout or claiming its broader story complete.\n\nRemove REBASE_HEAD from the set of operation blockers; preserve rebase-merge, rebase-apply, sequencer and all existing real lock/operation checks. A historical patch ref does not itself mean an operation is running. Validate with the existing real interrupted-rebase regression, the contributed stale-marker regression, and the full repository gate. Cleanup continues through normal finish and exact-id dry-run/apply, never by deleting Git metadata.\n\nThe broader story's doctor notes and marker-specific error presentation remain outside this correction.\n","title":"Allow retirement when only a historical REBASE_HEAD remains"},"args":{"command":"create-entity","data":{"body":"## Bounded correction\n\nThe current disk cleanup reproduced 15 false lock refusals whose only worktree-local operation marker is REBASE_HEAD. An existing independently owned managed checkout contains the narrow Git-adapter correction and regression test. Reuse that reviewed patch in this integration branch without changing its original checkout or claiming its broader story complete.\n\nRemove REBASE_HEAD from the set of operation blockers; preserve rebase-merge, rebase-apply, sequencer and all existing real lock/operation checks. A historical patch ref does not itself mean an operation is running. Validate with the existing real interrupted-rebase regression, the contributed stale-marker regression, and the full repository gate. Cleanup continues through normal finish and exact-id dry-run/apply, never by deleting Git metadata.\n\nThe broader story's doctor notes and marker-specific error presentation remain outside this correction.\n","status":"draft","title":"Allow retirement when only a historical REBASE_HEAD remains"},"entity_type":"aep.task/v1","locator":"ep://planning/store/task/stale-rebase-cleanup-correction"},"payload":{"actor":"human:timo","at":1788781569038,"causation":"cmd-new-task-stale-rebase-cleanup-correction","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"task:stale-rebase-cleanup-correction@1#0~453eba8fab3ab64f","executor":null,"recorded_at":"2026-09-07T11:46:09Z"}}
{"entity":"task","version":1,"id":"stale-rebase-cleanup-correction","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"implements":"story:rebase-head-is-not-a-lock"}]},"args":{"command":"create-relation","kind":"implements","source":"01MEM0000000000000017","target":"01MEM0000000000000004"},"payload":{"actor":"human:timo","at":1788781569052,"causation":"cmd-task-stale-rebase-cleanup-correction-implements-story-rebase-head-is-not-a-lock","change":{"change":"related","relation":"implements","target":"story:rebase-head-is-not-a-lock"},"correlation":"protocol-artifact-relate","event_id":"task:stale-rebase-cleanup-correction@1#0~818329a907072fb5","executor":null,"recorded_at":"2026-09-07T11:46:09Z"}}
{"entity":"task","version":1,"id":"stale-rebase-cleanup-correction","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000013","to":"proposed"},"payload":{"actor":"human:timo","at":1788781592334,"causation":"cmd-move-task-stale-rebase-cleanup-correction-1788781592334","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"task:stale-rebase-cleanup-correction@2#0~035cef12cd9405cc","executor":null,"recorded_at":"2026-09-07T11:46:32Z"}}
{"entity":"task","version":1,"id":"stale-rebase-cleanup-correction","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000013","to":"active"},"payload":{"actor":"human:timo","at":1788781592476,"causation":"cmd-move-task-stale-rebase-cleanup-correction-1788781592476","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"task:stale-rebase-cleanup-correction@3#0~489ac3135f862260","executor":null,"recorded_at":"2026-09-07T11:46:32Z"}}
{"entity":"task","version":1,"id":"stale-rebase-cleanup-correction","revision":3,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"/home/timo/.cache/disk-audit-20260907-414yqwj_/worktree-inspect-final-gate.log","source":"task check: 70 tests, including stale marker and real interrupted rebase; exit 0","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788781693000,"causation":"cmd-evidence-task-stale-rebase-cleanup-correction-test_result-1788781693600","change":{"change":"evidence","kind":"test_result","reference":"/home/timo/.cache/disk-audit-20260907-414yqwj_/worktree-inspect-final-gate.log","source":"task check: 70 tests, including stale marker and real interrupted rebase; exit 0"},"correlation":"protocol-artifact-evidence","event_id":"task:stale-rebase-cleanup-correction@3#0~f420129fc395212f","executor":null,"recorded_at":"2026-09-07T11:48:13Z"}}
{"entity":"task","version":1,"id":"stale-rebase-cleanup-correction","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000013","to":"implemented"},"payload":{"actor":"human:timo","at":1788781693950,"causation":"cmd-move-task-stale-rebase-cleanup-correction-1788781693950","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"task:stale-rebase-cleanup-correction@4#0~7b149e8637f603a1","executor":null,"recorded_at":"2026-09-07T11:48:13Z"}}
{"entity":"task","version":1,"id":"release-worktree-0-4-0","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Intent\n\nRelease Worktree 0.4.0 with native storage inspection and the stale REBASE_HEAD correction. Make the generated skill explicitly maintain leases when host hooks are absent, retain useful evidence separately from disposable build output, and finish with cleanup or an owned handoff. Authenticate the CI Task installer to avoid anonymous GitHub rate limits.\n\n## Acceptance\n\n- Existing inspection and cleanup safety tests and the complete repository gate pass.\n- The generated skill agrees with the CLI and documents lease release before finish.\n- Versions, changelog, and install instructions agree on 0.4.0.\n- Publish the reviewed source on main and an annotated 0.4.0 release; deliver public documentation through Website.\n\n## Authorization\n\nThe operator requested implementation and publication of the Worktree and Agentplugins updates on 2026-09-07. Larger resource budgets and native work-item associations remain separate work.\n","owner":"codex-hygiene-release","title":"Release native storage inspection and explicit lifecycle guidance"},"args":{"command":"create-entity","data":{"body":"## Intent\n\nRelease Worktree 0.4.0 with native storage inspection and the stale REBASE_HEAD correction. Make the generated skill explicitly maintain leases when host hooks are absent, retain useful evidence separately from disposable build output, and finish with cleanup or an owned handoff. Authenticate the CI Task installer to avoid anonymous GitHub rate limits.\n\n## Acceptance\n\n- Existing inspection and cleanup safety tests and the complete repository gate pass.\n- The generated skill agrees with the CLI and documents lease release before finish.\n- Versions, changelog, and install instructions agree on 0.4.0.\n- Publish the reviewed source on main and an annotated 0.4.0 release; deliver public documentation through Website.\n\n## Authorization\n\nThe operator requested implementation and publication of the Worktree and Agentplugins updates on 2026-09-07. Larger resource budgets and native work-item associations remain separate work.\n","owner":"codex-hygiene-release","status":"draft","title":"Release native storage inspection and explicit lifecycle guidance"},"entity_type":"aep.task/v1","locator":"ep://planning/store/task/release-worktree-0-4-0"},"payload":{"actor":"human:timo","at":1788783389638,"causation":"cmd-new-task-release-worktree-0-4-0","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"task:release-worktree-0-4-0@1#0~b56b3c221442ae9b","executor":null,"recorded_at":"2026-09-07T12:16:29Z"}}
{"entity":"task","version":1,"id":"release-worktree-0-4-0","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"delivers":"story:native-worktree-storage-inspection"}]},"args":{"command":"create-relation","kind":"delivers","source":"01MEM0000000000000022","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788783389969,"causation":"cmd-task-release-worktree-0-4-0-delivers-story-native-worktree-storage-inspection","change":{"change":"related","relation":"delivers","target":"story:native-worktree-storage-inspection"},"correlation":"protocol-artifact-relate","event_id":"task:release-worktree-0-4-0@1#0~01286a118800aa7e","executor":null,"recorded_at":"2026-09-07T12:16:29Z"}}
{"entity":"task","version":1,"id":"release-worktree-0-4-0","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000013","to":"proposed"},"payload":{"actor":"human:timo","at":1788783390145,"causation":"cmd-move-task-release-worktree-0-4-0-1788783390145","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"task:release-worktree-0-4-0@2#0~035cef12cd9405cc","executor":null,"recorded_at":"2026-09-07T12:16:30Z"}}
{"entity":"task","version":1,"id":"release-worktree-0-4-0","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000013","to":"active"},"payload":{"actor":"human:timo","at":1788783390160,"causation":"cmd-move-task-release-worktree-0-4-0-1788783390160","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"task:release-worktree-0-4-0@3#0~489ac3135f862260","executor":null,"recorded_at":"2026-09-07T12:16:30Z"}}
{"entity":"task","version":1,"id":"release-worktree-0-4-0","revision":3,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"/home/timo/.cache/hygiene-release-20260907-4tts5tr9/worktree-check.log","source":"task check: formatting, Clippy, 70 tests and generated skill consistency passed","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788783663000,"causation":"cmd-evidence-task-release-worktree-0-4-0-test_result-1788783663902","change":{"change":"evidence","kind":"test_result","reference":"/home/timo/.cache/hygiene-release-20260907-4tts5tr9/worktree-check.log","source":"task check: formatting, Clippy, 70 tests and generated skill consistency passed"},"correlation":"protocol-artifact-evidence","event_id":"task:release-worktree-0-4-0@3#0~1d613b99d1d4b66d","executor":null,"recorded_at":"2026-09-07T12:21:03Z"}}
{"entity":"task","version":1,"id":"release-worktree-0-4-0","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000013","to":"implemented"},"payload":{"actor":"human:timo","at":1788783664250,"causation":"cmd-move-task-release-worktree-0-4-0-1788783664250","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"task:release-worktree-0-4-0@4#0~7b149e8637f603a1","executor":null,"recorded_at":"2026-09-07T12:21:04Z"}}
{"entity":"story","version":1,"id":"scope-flag-for-reporting-subcommands","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"## Problem\n\n`worktree gc --repo <path>` and `worktree status` both report far more than the repository the\noperator is working in, and nothing in their output marks the difference.\n\n`--repo` is documented as \"Repository used to select its activated workspace policy\"\n(`worktree gc --help`), so it resolves a profile from `~/.config/worktree/config.toml` and then\nassesses every record under that profile's `workspace_root`. Observed on 2026-09-02 with\n`b10x-worktree-cli` 0.3.1:\n\n- `worktree gc --repo /home/timo/beyond10x/agentplugins --dry-run` returned 31 assessments, 28 of\n  them eligible, spanning atlas, connectors, aep, ess, harness, todo, website, service-sdk and\n  devcenter. One belonged to agentplugins.\n- `worktree gc --repo /home/timo/acme/projects/tenant-one/acd --dry-run` returned 1 assessment, and\n  that record's `repository_root` was `/home/timo/acme/projects/devcenter` — a different\n  repository inside the same `acme` profile, an adopter's internal collection of repositories.\n- `worktree status --help` lists one option, `--json`. It returned all 297 records across the\n  `b10x`, `acme` and `default` tree roots.\n\nAn agent following the generated skill's own step — run the dry-run and inspect every result —\ntherefore reads a list dominated by other repositories' trees. An `--apply` issued without exact\n`--id` values would remove another repository's work.\n\n## Proposal\n\n1. Add a `--scope` flag with three values:\n   - `repo` — only records whose `repository_root` is the resolved repository.\n   - `profile` — every record under the resolved profile's `workspace_root`. This is today's\n     behaviour.\n   - `global` — every record in the registry, across every profile.\n2. Default `--scope` to `repo` when the command is invoked from inside a repository, so the common\n   case narrows without a flag.\n3. Apply it to `gc`, `status` and `reconcile` at least.\n4. Leave `--repo` meaning what it means today — profile resolution. `--scope` decides how much of\n   that profile is reported.\n\n## Acceptance\n\n- `worktree gc --dry-run` from inside a repository assesses only that repository's records.\n- `--scope profile` reproduces today's output byte for byte on the same registry.\n- `--scope global` on `status` returns every record, matching today's unfiltered `status`.\n- `worktree status --scope repo` in a repository with no managed trees returns an empty set rather\n  than the whole registry.\n- The generated skill states the default and the flag.\n\n## Evidence\n\nWorktree 0.3.2 documents the current behaviour in the generated skill\n(`crates/worktree-cli/src/main.rs`, \"Finish and clean up\" step 3 and \"Audit and recovery\"). It\nrecords the limitation; it does not change it.\n"},"args":{"changes":{"body":"## Problem\n\n`worktree gc --repo <path>` and `worktree status` both report far more than the repository the\noperator is working in, and nothing in their output marks the difference.\n\n`--repo` is documented as \"Repository used to select its activated workspace policy\"\n(`worktree gc --help`), so it resolves a profile from `~/.config/worktree/config.toml` and then\nassesses every record under that profile's `workspace_root`. Observed on 2026-09-02 with\n`b10x-worktree-cli` 0.3.1:\n\n- `worktree gc --repo /home/timo/beyond10x/agentplugins --dry-run` returned 31 assessments, 28 of\n  them eligible, spanning atlas, connectors, aep, ess, harness, todo, website, service-sdk and\n  devcenter. One belonged to agentplugins.\n- `worktree gc --repo /home/timo/acme/projects/tenant-one/acd --dry-run` returned 1 assessment, and\n  that record's `repository_root` was `/home/timo/acme/projects/devcenter` — a different\n  repository inside the same `acme` profile, an adopter's internal collection of repositories.\n- `worktree status --help` lists one option, `--json`. It returned all 297 records across the\n  `b10x`, `acme` and `default` tree roots.\n\nAn agent following the generated skill's own step — run the dry-run and inspect every result —\ntherefore reads a list dominated by other repositories' trees. An `--apply` issued without exact\n`--id` values would remove another repository's work.\n\n## Proposal\n\n1. Add a `--scope` flag with three values:\n   - `repo` — only records whose `repository_root` is the resolved repository.\n   - `profile` — every record under the resolved profile's `workspace_root`. This is today's\n     behaviour.\n   - `global` — every record in the registry, across every profile.\n2. Default `--scope` to `repo` when the command is invoked from inside a repository, so the common\n   case narrows without a flag.\n3. Apply it to `gc`, `status` and `reconcile` at least.\n4. Leave `--repo` meaning what it means today — profile resolution. `--scope` decides how much of\n   that profile is reported.\n\n## Acceptance\n\n- `worktree gc --dry-run` from inside a repository assesses only that repository's records.\n- `--scope profile` reproduces today's output byte for byte on the same registry.\n- `--scope global` on `status` returns every record, matching today's unfiltered `status`.\n- `worktree status --scope repo` in a repository with no managed trees returns an empty set rather\n  than the whole registry.\n- The generated skill states the default and the flag.\n\n## Evidence\n\nWorktree 0.3.2 documents the current behaviour in the generated skill\n(`crates/worktree-cli/src/main.rs`, \"Finish and clean up\" step 3 and \"Audit and recovery\"). It\nrecords the limitation; it does not change it.\n"},"command":"update-entity","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1789475533272,"causation":"cmd-protocol-artifact-body-section-story-scope-flag-for-reporting-subcommands-1789475533272","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:scope-flag-for-reporting-subcommands@2#0~07a41d24e690f020","executor":null,"recorded_at":"2026-09-15T12:32:13Z"}}
